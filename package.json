{
  "name": "screenshare-p2p",
  "version": "1.0.0",
  "description": "Ultra low latency 1080p P2P screen sharing with audio for couples",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build-windows": "npx @neutralinojs/neu build && powershell -Command \"Get-ChildItem -Path dist\\StreamGrid -Include *linux*, *mac* | Remove-Item -Force; Compress-Archive -Path dist\\StreamGrid -DestinationPath dist\\StreamGrid-Windows.zip -Force\""
  },

  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  }
}
